---
title: notion-to-jsx
---

import { Tabs } from 'nextra/components'

# notion-to-jsx

Notion 페이지 블록을 React 컴포넌트로 렌더링합니다.

## Features

- **공식 Notion API** 기반 — `@notionhq/client` 확장
- **자동 메타데이터 주입** — 이미지 크기, 북마크 OG 데이터를 페칭 시 자동 처리
- **17개 블록 타입** 지원 — 텍스트, 리스트, 이미지, 코드, 테이블, 토글, 비디오, 임베드 등
- **다크 모드 / 목차** — 내장 테마와 자동 생성 목차
- **Zero-runtime CSS** — Vanilla Extract 기반 스타일링

## 설치

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tabs.Tab>
    ```bash copy
    pnpm add notion-to-utils notion-to-jsx react react-dom
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    npm install notion-to-utils notion-to-jsx react react-dom
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    yarn add notion-to-utils notion-to-jsx react react-dom
    ```
  </Tabs.Tab>
</Tabs>
