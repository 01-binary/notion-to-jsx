---
title: Quick Start
---

import { Steps, Tabs } from 'nextra/components'

# Quick Start

## Start with a template

Use the blog template with category filtering, dark mode, RSS, and more.

<Steps>

### Create from template

Click **Use this template** → **Create a new repository** on [notion-to-jsx-template](https://github.com/01-binary/notion-to-jsx-template)

### Set environment variables

Clone your new repo and configure environment variables.

```bash copy
cp .env.example .env.local
```

```bash copy filename=".env.local"
NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxx
NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### Run

```bash copy
pnpm install && pnpm dev
```

</Steps>

## Manual installation

To add to an existing Next.js project:

### Requirements

- **Node.js** 18 or later
- **React** 19+
- **Next.js** 14+ (recommended, for server components)

### Install packages

<Tabs items={['pnpm', 'npm', 'yarn']}>
  <Tabs.Tab>
    ```bash copy
    pnpm add notion-to-utils notion-to-jsx
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    npm install notion-to-utils notion-to-jsx
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash copy
    yarn add notion-to-utils notion-to-jsx
    ```
  </Tabs.Tab>
</Tabs>

### CSS setup

Import the styles in your root layout:

```tsx copy filename="app/layout.tsx"
import 'notion-to-jsx/dist/index.css';
import 'prismjs/themes/prism-tomorrow.css'; // code syntax highlighting (optional)
```

> [!IMPORTANT]
> Import order matters: `notion-to-jsx/dist/index.css` must come **before** the Prism.js theme and your custom styles, so that overrides work correctly.

### Minimal example

<Steps>

### Setup the Notion client

```ts copy filename="lib/notion.ts"
import { Client } from 'notion-to-utils';

export const notionClient = new Client({
  auth: process.env.NOTION_TOKEN,
});
```

### Fetch blocks on the server

```tsx copy filename="app/posts/[id]/page.tsx"
import { notionClient } from '@/lib/notion';
import PostRenderer from './PostRenderer';

export default async function PostPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;

  const [blocks, properties] = await Promise.all([
    notionClient.getPageBlocks(id),
    notionClient.getPageProperties(id),
  ]);

  const title = (properties?.['Name'] as string) || '';
  const cover = (properties?.['coverUrl'] as string) || '';

  return <PostRenderer blocks={blocks} title={title} cover={cover} />;
}
```

### Render with a client component

```tsx copy filename="app/posts/[id]/PostRenderer.tsx"
'use client';

import { Renderer } from 'notion-to-jsx';
import type { NotionBlock } from 'notion-to-jsx';

interface Props {
  blocks: NotionBlock[];
  title?: string;
  cover?: string;
}

export default function PostRenderer({ blocks, title, cover }: Props) {
  return (
    <Renderer
      blocks={blocks}
      title={title}
      cover={cover}
      showToc
    />
  );
}
```

</Steps>

The `Renderer` handles all block rendering, styling, and table of contents generation automatically.
